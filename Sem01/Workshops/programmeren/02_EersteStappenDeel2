Vragen vooraf aan het les:
Objecten zijn zoveel mogelijk gekoppeld aan echte dingen. Bijvoorbeeld een object vragen en een object enquete. Object Enquete bestaat dan uit meerden objecten vragen

programmeren is oefenen,oefenen, oefenen.

Voor dat je code gaat kloppen eerst via flowchart visueel maken (draw.io)

Les zelf:
Loop, niet 38000 keer op schrijven update x, maar via loop x 38000 keer updaten zodat je het maar 1x hoeft te schrijven.

string invoer, toetsenbord input is altijd een string

if ; else ; statement uitleg, is x waar of niet waar, eenmalig iets.

While, meer dan eenmalig

do while loep. do eerst iers en dan pas pas while doen. Dus niet eerst while en dan een fictieve do ingeven

Variable hebben houdbaarheid, dat valt binnen de {} waar die wordt aangeroepen, in c#

Boolean = true of false

c# tutorial in one hour
